# GCFIS — changelog (version-only)
s45  baseline v40 audit + GCFIS 13-layer spec
s46  change-core + first engines (fragility, forward-macro, accumulation, lead-lag, meta) + real-data validation
s47  full 13-layer GCFIS + Entry engine (gamma-aware, risk-range) + dead-code prune 122→103
s48  cross-asset coherence + per-ticker reason narrative + entry defer-gate + GCFIS dashboard renderer
s49  GCFIS wired into app — new 🧭 GCFIS tab (router + snap-adapter, defensive), sidebar caption trimmed to version-only
s50  product-confluence ranking (geomean theme×bottleneck×accumulation×adoption×reflexivity) + bottleneck node→ticker mapping + Reflexivity engine (B5) + full per-ticker output contract (Options/Macro/Opportunity scenarios/Institutional) + multi-panel card
s51  complete output contract: Options full greek set (gamma + charm + gamma_flip + vanna), Scores panel adds liquidity/dealer/positioning, Institutional threads revision/ownership_Δ/etf_flow + fundamentals into accumulation crowding
s52  lead-lag graph wired INTO selection (rotation engine: fired-leader → primed-follower conviction boost + timing window) — the GCFIS 'moat', previously decorative; portfolio concentration guard (correlation clustering → effective bets + alloc multiplier so a correlated cluster isn't oversized); leadlag_cfg passthrough
s53  ALL 4 structural gaps closed + GCFIS now VISIBLE/POPULATED: (1) real Gaussian HMM regime engine (regime_hmm.py, hmmlearn, runtime-fit, graceful fallback) replaces quad-lookup posterior; (2) bottleneck migration tracker (emerging/fading node by Δtightness) wired into orchestrator output; (3) adapter ENRICHED — harvests v40's real data (volumes, breadth, VIX, GEX/walls→dealer_by_ticker override, quads) so panels populate instead of n/a (v40 GEX labelled proxy, not faked); (4) GCFIS confluence section folded into Alpha Center (_gcfis_inline, guarded). requirements: +hmmlearn
s54  FIX long-only directional bug (the BREN.JK screenshot): rich_ticker_card _gex_levels_chart drew a SHORT layout (Entry at resistance ABOVE px, target BELOW entry) on buy-only IDX names, ignoring the already-correct WAIT recommendation → now long-only aware (bearish IDX → range/flat, Entry below px, target ABOVE entry, 'buy-only' title) + stale-entry guard ('harga sudah lewat entry'); _directional_levels + compute_optimal_entry + build_options_recommendation all long-only guarded. GCFIS: market registry (markets.py — doc 5: per-market long_only/drivers/bottlenecks) + entry long_only short→AVOID + orchestrator avoided_long_only bucket + market stamped per signal + dashboard AVOID section.
s55  docs 1-9 COMPLETE: flow_type engine (4-way ACCUMULATION/DISTRIBUTION/SHORT_COVERING/PANIC + absorption/efficiency/persistence/resilience, OHLCV-proxy labelled) + market_mode engine (PINNING/EXPANSION/SQUEEZE/DISTRIBUTION → execution style) + Stage-1 elimination (MAD-robust gaps/vol-of-vol/false-breakout) + response_zone (TRR/LRR as response zones) + decision_stack per ticker (Category/Why-Now/Who's-Trapped/Invalidation/Execution+size+holding+target-map) + market-weighted confluence (per-market driver weights, doc 5) + doc-4 sections (Early Monsters/Squeeze/Tactical/MeanRev/Distribution Warning) + researched MARKET DRIVER MAP (market_drivers.py + MARKET_DRIVERS.md: surge-up/down per market, data series, horizon ST/MT/LT, strength, Jun-2026 evidence) wired into tab with live z-readings from available feeds
s56  BandarMetrics REDESIGN shipped (engines/flow_regime.py: Corr_F/Par_F/EFD + gated LPM + regime classifier FOREIGN_LED/DOMESTIC_LED/OPERATOR/DECOUPLED; Vol Rotation/AvgCost/NetBSF dropped; idx wiring via typef_by_ticker → flow01 override + decision-stack + BM card chips); orchestrator local-numpy-import scoping bug fixed; MQA_v26.pine: response-zone engine at TRADE band (RECLAIM/ABSORPTION/ACCEPTANCE/NO-BID/REJECTION) + path-dependency (GRIND vs SPIKE into band) + table row + 4 alerts (additive — L1 ranges/multipliers untouched)
s57  FINAL 6-TAB DESIGN LIVE (attachment-1): 🛰 Mission Control (system-state summary + stress composite[prior] + divergence early-warnings + top-EV cards) · 🌐 Regime & Flow (macro pulse + driver map + relative/internals + lead-lag) · ⚡ Opportunity (doc-4 sections + alpha-center/themes folded) · 🗺 Market Intelligence (5 market subtabs) · 🔬 Ticker Intelligence (card + horizon/chain/dealer/flow/response/BM panels per market) · 📊 Portfolio & Scenario (stress + concentration + scenario-lite 120d-beta[labeled] + Research Lab). New engines: internals.py (relative pairs z20, divergence engine, breadth/top5 dispersion-lite), run_horizon (multi-TF alignment), adaptive stress weight tilt in confluence (flow/accum up, theme/reflexivity down on deleveraging/risk-off). Old 10-tab nav retired (legacy pages remain as components inside new tabs).
s58  SCREENSHOT-AUDIT FIXES + DOC 15-23. (1) Conviction collapse fixed: removed ticker-independent floor 100·stress·W_short (the literal 69.5 on every card); stress now amplifies per-ticker bear evidence ×(1+0.6·stress); BUILD_SHORT gated on bear≥35; contradiction guard: ACCUMULATION/RECLAIM tape caps short ×0.45 + _short_conflict. dist fed by microstructure (flow DIST +0.30, PANIC +0.15, mode DIST +0.20, REJECTION/NO_BID +0.15). (2) HYBRID conviction (adopted from external-AI critique): conviction = 0.30·meta + 0.25·confluence + 0.15·flow01 + 0.10·horizon + 0.10·responseQ + 0.10·crowd-term (direction-aware inversion); positioning override crowd>90&vel<0 → meta_long×0.30. (3) EV per signal + EV chip + Mission Top-EV valid-first EV-sorted. (4) Decision stack direction-aware (shorts carry only bearish evidence, ⚠ conflicts surfaced). (5) SURGE engine (doc-20, 8 layers, priors) + sig.surge; CRASH/BOTTOM engine (doc-21/22): pressure, FLUSH/CYCLICAL/SYSTEMIC classifier (credit feed = seam, labeled), local-vs-durable bottom. (6) UI doc-15/16: scan-first cards (<details>), Mission Control = WORLD STATE → macro strip → OPPORTUNITY MATRIX (LONG/SHORT/EARLY/HEDGE) → FRAGILITY STACK; Ticker Intelligence = 3-col THESIS/POSITIONING/EXECUTION + why-market-wrong + dealer ladder + CONFIDENCE STACK + crypto archetypes (doc-17). (7) LEAN_* low-conf driver bias, AVOID cosmetics, BM panel Vol Rotation/AvgCost removed, market_intel engine headers, BEST driver-bias fallback, FLAWS_AUDIT.md (doc-23, 18 flaws, honest status). Suite 31/31 incl non-vacuous t_no_blanket_short + t_surge_crash.
s59  REAL FEEDS + VISUAL LAYER (screenshot audit round-2). (1) feeds/fred_feed.py: FRED via fredgraph CSV (no key) → NetLiq = WALCL/1000 − WTREGEN − RRPONTSYD ($bn) + DFII10 → driver ids FEDLIQ/TIPS10Y (level; read_all z's the change). (2) feeds/typef_idx.py: REAL Type-F from IDX GetStockSummary (ForeignBuy/ForeignSell = VALUE) → per-.JK df matching FlowRegimeEngine REQUIRED (open/high/low/close/volume/fb/fs+total_value); per-day CSV cache (/tmp/typef_cache), tolerant key-casing parser; warm-up ~120 sessions once. Both wired into the single cached GCFIS run (_gcfis_inline) with session caching + status lines on Mission Control; parsers fixture-tested (NetLiq math, 2 key casings, engine smoke) — live fetch verifiable on deploy only (sandbox blocks hosts). (3) BM panel REAL mode: when series_real present → Price + LPM + Foreign-Flow cum (secondary) / Intensity / NetF bars — same composition as the reference BM app; Corr_F=corr(close, cumΣFN), Par_F=(FB+FS)/2V identical definitions, directly comparable; proxy panel retitled "(PROXY — Type-F absent)". LPM remains OUR documented (close−vwap)·vol cum, not BM's proprietary formula (labeled). (4) Mission strip garbage fixed: orchestrator exports systemic_flat (the SAME numbers the engines consume); num() first-numeric heuristic retired for these reads; regime line reads _regime_method (was wrong key → "—"); SHORT/HEDGE dedupe; feeds status captions; scan-card conv nowrap. (5) regime_hmm heuristic fallback is now GIP-aware: blends trend-heuristic 50/50 with the system's own monthly-quad prior (Q2→risk_on-tilt) → blind risk_off no longer nukes every long; method labeled heuristic_fallback+gip. (6) Visuals: components/mini_viz.hbar (guarded) → crash-components chart (Mission), confidence-stack chart (Ticker), relative-pairs chart (Regime&Flow); GEX plane with no chain → compact LEVEL LADDER (h=200, y hidden) instead of empty scatter. Caption → s59. Suite 32/32 incl t_feeds_parsers.
s60  FINAL DESK + MISSED-ITEMS SWEEP (per-doc gap audit after user callout). (1) meta/final_desk.py — THE answer, not inventory: ≤10 cross-market picks; hard validity bar per pick (actionable + entry_valid + EV present + ≥2 evidence lines + invalidation + short-conflict guard); desk_score = 0.45·tanh(EV/10)+0.30·conv+0.15·surge+0.10·responseQ (priors); diversity caps ≤3/market ≤2/theme; thin book → output N<10 with explicit "no fabricated fills" (no quota padding); rejected_summary surfaced. Mounted as the TOP panel of Mission Control AND Opportunity. Adaptive primary target (doc-17): risk_on>0.5 long → stretch target, else base. (2) Causal propagation map (doc-16 "most important visual", previously skipped entirely): components/causal_map.py — 4 researched chains (LIQUIDITY/OIL SHOCK/AI CAPEX/IDX FLOW) as a directed plotly graph; node size/color = LIVE |z| from driver feeds, dim = honest feed seam, red end-node border at crash≥60; mounted on Mission + Regime&Flow. (3) IHSG doc-16 items previously skipped: FALSE-ACCUMULATION alert (lpm_slope_z>0.5 & liq_expand<0.98 from flow_regime — REAL fields, now exposed via latest()) → scan-card chip + ticker warning + IHSG-tab trap list; participation-quality readout (pressure breadth). Exit-stress meter remains a SEAM (needs depth/DTE data — not faked). (4) Doc-18 relative engine completion: copper/gold + btc/eth pairs + AUDJPY single-series z (carry/risk-appetite barometer). (5) Doc-16 narrative lifecycle map: per-theme crowding×velocity scatter with EMERGING/INSTITUTIONAL/CROWDED/MANIA bands (vel<0 = exhaustion). Suite 33/33 incl t_final_desk (validity bar, sort, diversity, rejection set, thin-book honesty).
