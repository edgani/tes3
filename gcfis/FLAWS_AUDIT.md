# FLAWS AUDIT — doc-23 (18 flaws) → implementation status (s58, honest)
1. Reflexivity paradox — PARTIAL: runaway/parabolic = EXIT; surge caps reflexivity at 80. Organic-vs-reflexive demand separation needs flow feeds (seam).
2. Latency — SEAM: high-frequency proxies (trucking, card spend, datacenter power) need feeds.
3. False causality — PARTIAL: driver map uses researched causal signs + horizons; conviction weight order encodes hierarchy. No formal causal inference.
4. Regime fragility — DONE: regime posterior gates everything first; adaptive stress weight tilt.
5. Positioning override — DONE: crowd>90 & vel<0 → meta_long×0.30 regardless of macro.
6. Second-order propagation — PARTIAL: theme→bottleneck_node→ticker chain shown; full supply-chain graph = seam (bottleneck_nodes param).
7. Time-horizon conflict — DONE: ST/MT/LT driver labels + multi-TF alignment engine + per-card hold.
8. Data confidence — PARTIAL: LEAN_* low-confidence bias labels, proxy labels on flow/GEX/scenario. No numeric confidence score yet.
9. Narrative distortion — PARTIAL: features are pre-move (flow/compression/positioning); narrative NLP discovery = seam.
10. Lookahead — DONE by construction in engines/tests; live feeds must be point-in-time (operator responsibility, documented).
11. Overfitting — DONE as policy: all composite weights labeled PRIORS; validation gate = DSR≥0.95 + perm_p<0.05 else NOISE.
12. Tail-risk — PARTIAL: scenario-lite (120d beta, labeled) + crash engine; full stress simulation = seam.
13. Edge decay — SEAM: tracker not built.
14. Policy shock — SEAM: policy-risk layer needs feeds (OPEC/BI/tariffs).
15. Local-bottom misclassification — DONE: bottom engine separates LOCAL_BOUNCE_RISK vs DURABLE_BOTTOM_FORMING (6 stabilization checks).
16. Cross-market desync — DONE: cross-asset coherence engine + internals divergence engine (index-vs-breadth, pairs).
17. Bottleneck misidentification — PARTIAL: node chain surfaced; graph engine = seam.
18. Signal collision — DONE: priority hierarchy encoded in conviction weights (liquidity/regime > positioning > flow > horizon/technicals); credit layer = seam.
